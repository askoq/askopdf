name: asko_pdf
description: "Lightweight PDF Viewer"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  crypto: ^3.0.6
  ffi: ^2.1.0
  file_picker: ^11.0.2
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  desktop_drop: ^0.7.0
  pasteboard: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true